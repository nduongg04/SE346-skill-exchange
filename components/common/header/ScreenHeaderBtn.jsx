import { View, Text } from "react-native";
import { COLORS } from "@constants";
const ScreenHeaderBtn = () => {
	let color = COLORS.lightWhite;

	return (
		<View>
			<Text>ScreenHeaderBtn</Text>
		</View>
	);
};

export default ScreenHeaderBtn;
